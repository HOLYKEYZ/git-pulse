import { describe, expect, it } from 'vitest';

describe('button', () => {
  it('should render', () => {
    expect(true).toBe(true);
  });
});import { describe, expect, it } from 'vitest'

describe('Button', () => {
  it('should render', () => {
    expect(true).toBe(true)
  })
})