{
  "name": "gitpulse",
  "private": true,
  "scripts": {
  "test": "vitest",
  "dev": "pnpm --filter web dev",
  "build": "pnpm --filter web build",
  "lint": "pnpm --filter web lint"
    "test": "vitest"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": "*"
  }
}